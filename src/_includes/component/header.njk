<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Header</title>
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        .header {
            display: flex;
            width: 100%;
            justify-content: flex-start;
            align-items: center;
            background-color: #ECE2DC;
            padding: 10px 20px;
            
            
        }
    </style>
</head>
<body>
    <div class="header">
    
        <div class="logo" style="--d: flex; --ai: center; --jc: flex-start;">
        <img style="--h: 60px; --mr: 10px;" src="/assets/logo.png" alt="Logo">
        <div style="--size: 21px; --weight: bold; --ff: 'Times New Roman', Times, serif; --ls: 0.05em; --size-lg: 25px; --ls-lg: 0.3em;">
        INSPECTION SEASON
        <div style="--size: 13px; --weight: bold; --ff: 'Times New Roman', Times, serif; --ls: 0.05em; --size-lg: 18px; --ls-lg: 0.2em;"> 
        Inspect with Confidence Every Season
        </div>
        </div>
        
        </div>
       
       
    </div>

    <!-- Additional content goes here -->
</body>